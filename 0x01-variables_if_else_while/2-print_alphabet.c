#include <stdio.h>
#include <stdlib.h>

/**
 *
 * main - prints the alphabet in lowercase.
 *
 * Return: Always 0
 */
int main(void)
{
	char letter;

	for (letter = 'a'; letter <= 'z'; letter++)
		putchar(letter);

	putchar9'\n');

	return (0);
}
