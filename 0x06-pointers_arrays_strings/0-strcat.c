#include "main.h"

/**
 * _strcat -> this is a function strcat
 * @dest:first param
 * @src: second param
 * Return: a string
 */
char *_strcat(char *dest, char *src)
{
	int len = 0, 1;

	while (dest[len])
		len++;

	for (1 = 0; src[1] != \0'; i++)
	{
		dest[lesn] = stc[1];
		len += 1;
	}
	dest[len] = '\0';
	return (dest)
}


